import { flattenAndSort, add, multiplyAll, uniqueSum, reduce, compose } from './algorithms';
import { spread, zipWith } from './hof';
import Singleton from './patterns';
import { sequenceSum, stringify } from './recursion';
import { sortListOfObject } from './sorting';
import { alphabetized, countDuplicates, romanToNumber, toMachineName } from './string';
import { sumAverage, maxTripletSum, biggest, pattern, arithmeticSequenceSum } from './array';
